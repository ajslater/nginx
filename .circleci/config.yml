version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker login -u="$DOCKER_USER" -p="$DOCKER_PASS"
      - run: ./build.sh

workflows:
  build:
    jobs:
      build:
        post-steps:
          - run: ./tag-version.sh
          - run: ./tag-latest.sh
